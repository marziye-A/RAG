# a semanitic(faiss) and lexical(bm25) RAG system and a hybrid RAG system with a cross encoder reranker
## Requirements

See `install.sh`
```
pip install torch transformers accelerate faiss-cpu rank-bm25 bert-score tqdm numpy scipy
```

###
